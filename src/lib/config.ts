// Centralized configuration for the frontend

// Switch to "http://localhost:8080" to test with your local cluster
export const RPC_URL = process.env.NEXT_PUBLIC_RPC_URL || "https://blockchain-simulator-production.up.railway.app";
